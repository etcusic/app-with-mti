<h1>Students ranked by gold stars - because learning can be competitve too!</h1>
<ul>
     <% @students.each do |student| %>
        <li><%= link_to "#{student.full_name} - #{student.gold_stars}", student_path(student) %></li>
    <% end %>
</ul>

<br><br>

<div class='centered'>
    <button><%= link_to "Profile", current_user.url %></button>
    <button><%= link_to "Home Page", '/' %></button>
</div>