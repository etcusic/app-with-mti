<h3>There Be Some Problems, yo!</h3>
    <div>
        <ul>
            <% flash.each do |type, msg| %>
            <li><%= "#{type}: #{msg}" %></li>
            <% end %>
        </ul>
        <img s rc='/photos/sad_panda.gif'>
    </div>
    <h4>Refresh the page to move on.</h4>