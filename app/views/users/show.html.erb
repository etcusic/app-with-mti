<img class='profile-photo' src="<%= public_profile.image %>", alt="profile pic goes here">

<% if users_stuff? %>
    <%= render partial: "#{current_user.type.downcase}s/profile" %>
<% else %>
    <%= render partial: "#{public_profile.type.downcase}s/public" %>
<% end %>






