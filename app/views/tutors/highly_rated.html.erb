<h1>Highly rated tutors (50+ rating)</h1>
<ul>
     <% @tutors.each do |tutor| %>
        <li class='tutors'>
            <%= link_to "#{tutor.full_name}", user_path(tutor) %>
            (rating: <%= tutor.rating %>)<br>
            <img src="<%= tutor.image %>", alt="tutor photo here">
        </li>
    <% end %>
</ul>